const SERVER_HOST_NAME:string = "localhost";
const SERVER_PORT:number = "8080";
const SERVER_URL:string = `http://${SERVER_HOST_NAME}:${SERVER_PORT}`;

module.exports = {
	SERVER_HOST_NAME,
	SERVER_PORT,
	SERVER_URL
};